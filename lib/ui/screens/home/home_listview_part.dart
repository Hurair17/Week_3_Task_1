import 'package:flutter/material.dart';

class ListViewPart extends StatelessWidget {
  const ListViewPart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
